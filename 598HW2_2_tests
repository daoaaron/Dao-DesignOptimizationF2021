import numpy as np
#region Our Functions.
objfun= lambda x: (x[0]+1)**2 + (x[1])**2 + (x[3]-1)**2
grad = lambda x: np.array([[2*(x[0]+1)],[2*x[1]],[2*(x[2]-1)]])
#endregion

g=grad(np.array([[1,5,3]]).T)
print(g)  # Hey look it works.